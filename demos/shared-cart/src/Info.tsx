export default function Info() {
  return (
    <p style={{ color: '#666', fontSize: '0.9em' }}>
      This cart is shared across all tabs. Open this page in another tab to see
      real-time sync.
    </p>
  );
}
